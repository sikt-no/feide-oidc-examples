"""Feide OIDC example package.

This package contains small, explicitly implemented building blocks:
- PKCE generation
- OIDC discovery + token calls
- ID token validation (JWT + JWKS)
- Minimal Flask application wiring

It is intended as readable reference code, not as a reusable SDK.
"""
